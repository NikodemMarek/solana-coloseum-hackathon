{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "anchor build",
            "type": "shell",
            "command": "docker compose run anchor-builder anchor build",
            "options": {
                "cwd": "./ideas-marketplace"
            }
        },
        {
            "label": "anchor deploy",
            "type": "shell",
            "command": "docker compose run anchor-builder anchor deploy",
            "options": {
                "cwd": "./ideas-marketplace"
            }
        },
        {
            "label": "anchor test",
            "type": "shell",
            "command": "docker compose run anchor-builder anchor test",
            "options": {
                "cwd": "./ideas-marketplace"
            }
        },
        {
            "label": "anchor grow program",
            "type": "shell",
            "command": "docker compose run anchor-builder rm /workdir/target/deploy/ideas_marketplace-keypair.json",
            "options": {
                "cwd": "./ideas-marketplace"
            }
        }
    ]
}
